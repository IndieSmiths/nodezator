(A) add translation guides like the ones in the bionic blue game repo
(A) update colors viewer and picker
(B) update fonts viewer so user can preview specific char groups
x update file manager
x disable system testing for now (in the future it must be replaced by one that generates events, key presses and mouse actions on the fly, to account for size of nodes and other interfaces now that we'll allow users to pick fonts and their sizes)
x add a "reset to defaults" button to "language and fonts" form
